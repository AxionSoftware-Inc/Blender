# Chat-limit continuation pointer

Canonical continuation document for the 2026-09-06 Spectra development session:

- `docs/CHAT_LIMIT_HANDOFF_2026-09-06.md`

Read that file first when a new assistant, agent, or chat resumes work. It contains the verified baseline, pending validation stack, presentation architecture, Maxwell and Electrostatic showcase state, Quantum audit state, validation gates, continuation order, capability summary, limitations, and exact first actions.

Do not infer that current `main` is green merely because the implementation files exist. Re-establish the current HEAD and run the validation gates described in the canonical handoff before promoting a new verified checkpoint.
